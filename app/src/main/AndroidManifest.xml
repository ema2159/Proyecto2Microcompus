<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="example.emmanuel.proyecto2microcompus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".coffeactivity"
            android:label="@string/title_activity_coffeactivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".resActivity"
            android:label="@string/title_activity_res"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".BarActivity"
            android:label="@string/title_activity_bar"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".sodActivity"
            android:label="@string/title_activity_sod"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>